#include <bits/stdc++.h>
#include "NumberSplit.hpp"

int main(){
    NumberSplit s;
    std::cout<<s.longestSequence(6)<<std::endl;
    std::cout<<s.longestSequence(97)<<std::endl;
    std::cout<<s.longestSequence(234)<<std::endl;
    std::cout<<s.longestSequence(876)<<std::endl;
    std::cout<<s.longestSequence(99999)<<std::endl;
    std::cout<<s.longestSequence(100000)<<std::endl;
}